@model IEnumerable<demoTraining.Models.TrainerEnrollment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<p>
    <a href="/TraineeEnrollments">Enroll for Trainee</a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Topic.TopicName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trainer.TrainerName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Topic.TopicName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trainer.TrainerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrainerEnrollmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrainerEnrollmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrainerEnrollmentID })
        </td>
    </tr>
}

</table>
