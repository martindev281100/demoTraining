@model IEnumerable<demoTraining.Models.TraineeEnrollment>

@{
    ViewBag.Title = "MyCourse";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>MyCourse</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.CourseName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trainee.TraineeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trainee.TraineeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TraineeEnrollments }) |
            @Html.ActionLink("Details", "Details", new { id=item.TraineeEnrollments }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TraineeEnrollments })
        </td>
    </tr>
}

</table>
